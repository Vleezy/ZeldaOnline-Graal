These files are needed for the Zelda game.
makeArrays and addSprites create the file "arrays.dat"
that contains some Integer arrays needed for the game.
Create that file:
1. Modify the arrays in the files
2. Compile them with "javac <filename>"
3. Start them: "java makeArrays", "java addSprites"
4. Copy arrays.dat to the game folder